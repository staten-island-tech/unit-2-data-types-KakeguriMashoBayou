age=24
isMember=False
isResident=False

def discount(age, isMember, isResident):    
    if age < 12 or age >= 65 :
     print("discount qualified") 
    elif isMember or isResident == True:
       print("discount qualified")
    else:
       print("no discount")
discount(age, isMember, isResident)